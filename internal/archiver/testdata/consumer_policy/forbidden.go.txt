package fixture

func metadataFixture() {
	consume("information_schema")
	consume("INNODB_FOREIGN")
	consume("KEY_COLUMN_USAGE")
	consume("REFERENTIAL_CONSTRAINTS")
	consume("ENABLED_ROLES")
	consume("USER_PRIVILEGES")
	consume("SCHEMA_PRIVILEGES")
	consume("TABLE_PRIVILEGES")
	consume("CURRENT_USER()")
	consume("TABLE_NAME, TABLE_TYPE, ENGINE")
	consume("TABLE_NAME, COLUMN_NAME, ORDINAL_POSITION, DATA_TYPE, COLUMN_TYPE, EXTRA")
	consume("TABLE_NAME, COLUMN_NAME, DATA_TYPE, COLUMN_TYPE")
	consume("TABLE_SCHEMA, TABLE_NAME, INDEX_NAME, COLUMN_NAME, SEQ_IN_INDEX")
	consume("EVENT_OBJECT_TABLE, TRIGGER_NAME, EVENT_MANIPULATION, ACTION_TIMING")
	consume("GRANTEE, TABLE_SCHEMA, PRIVILEGE_TYPE")
	consume("GRANTEE, TABLE_SCHEMA, TABLE_NAME, PRIVILEGE_TYPE")

	sqlmock.NewRows([]string{"TABLE_NAME", "TABLE_TYPE", "ENGINE"})
	sqlmock.NewRows([]string{"TABLE_NAME", "COLUMN_NAME", "ORDINAL_POSITION", "DATA_TYPE", "COLUMN_TYPE", "EXTRA"})
	sqlmock.NewRows([]string{"TABLE_NAME", "COLUMN_NAME", "DATA_TYPE", "COLUMN_TYPE"})
	sqlmock.NewRows([]string{"TABLE_SCHEMA", "TABLE_NAME", "INDEX_NAME", "COLUMN_NAME", "SEQ_IN_INDEX"})
	sqlmock.NewRows([]string{"EVENT_OBJECT_TABLE", "TRIGGER_NAME", "EVENT_MANIPULATION", "ACTION_TIMING"})
	sqlmock.NewRows([]string{"GRANTEE", "TABLE_SCHEMA", "PRIVILEGE_TYPE"})
	sqlmock.NewRows([]string{"GRANTEE", "TABLE_SCHEMA", "TABLE_NAME", "PRIVILEGE_TYPE"})
}
