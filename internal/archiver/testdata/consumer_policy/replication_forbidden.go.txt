package fixture

// SHOW SLAVE STATUS is legacy — comment-invisibility control, must never be flagged.
const (
	mixedCaseReplicaQuery = "Show Replica Status"
	lowercaseSlaveQuery   = "show slave status for channel 'billing'"
)
