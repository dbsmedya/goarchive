package archiver

import "github.com/dbsmedya/goarchive/internal/types"

// RecordSet is an alias for types.RecordSet to avoid import cycles
// and maintain backward compatibility with existing code.
type RecordSet = types.RecordSet
